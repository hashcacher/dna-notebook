# DNA Sequence Visualizer

This is a web-based application for storing and visualizing DNA sequences, both linear and circular.

## Setup

1. Install Python dependencies: `pip install -r requirements.txt`
2. Install Node.js dependencies: `cd frontend && npm install`
3. Start the Flask server: `python main.py`
4. Start the React development server: `cd frontend && npm start`

The application will be available at `http://localhost:3000`.